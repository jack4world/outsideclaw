# Website (GitHub Pages)

Goal: create a small official site to explain outsideclaw and help adoption.

## Recommended approach
Use **GitHub Pages** from the `docs/` folder (simple, no build step).

## Files
- `docs/index.md` landing page
- `docs/_config.yml` minimal Jekyll config

## Enable
In GitHub repo settings:
- Settings → Pages
- Source: Deploy from a branch
- Branch: `main` / folder: `/docs`

## Optional
- Add a custom domain later
- Add a short demo GIF/PNG under `docs/assets/`
